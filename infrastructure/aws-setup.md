# AWS Infrastructure Setup

All AWS provisioning steps have been consolidated into the main [README.md](../README.md).

Follow the README from Step 1 (Create SSH Key Pair) through Step 5 (Allocate Elastic IP) for infrastructure setup, then Step 12 (Provision NLB) after the ingress controller is installed.
